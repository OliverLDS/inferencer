% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_multimodal.R
\name{query_gemini_content}
\alias{query_gemini_content}
\title{Query a Gemini Multimodal Model}
\usage{
query_gemini_content(
  prompt = NULL,
  parts = NULL,
  api_key = Sys.getenv("GEMINI_API_KEY"),
  model = "gemini-2.5-flash",
  url0 = Sys.getenv("GEMINI_API_URL", unset =
    "https://generativelanguage.googleapis.com/v1beta/models"),
  temperature = 0.7,
  top_p = 1,
  top_k = 40,
  max_tokens = NULL,
  response_modalities = NULL,
  speech_config = NULL,
  json_list = FALSE
)
}
\arguments{
\item{prompt}{A non-empty character string. Ignored when \code{parts} is
supplied.}

\item{parts}{Optional Gemini \code{parts} payload supplied as a list. Use this for
multimodal inputs such as \code{inlineData} or \code{fileData}.}

\item{api_key}{Gemini API key. Defaults to \code{Sys.getenv("GEMINI_API_KEY")}.}

\item{model}{Model identifier.}

\item{url0}{Base Gemini models URL.}

\item{temperature}{Sampling temperature.}

\item{top_p}{Nucleus sampling parameter.}

\item{top_k}{Top-k sampling parameter.}

\item{max_tokens}{Optional maximum number of output tokens.}

\item{response_modalities}{Optional response modalities, for example
\code{c("TEXT")}, \code{c("AUDIO")}, or \code{c("TEXT", "IMAGE")}.}

\item{speech_config}{Optional Gemini \code{speechConfig} object supplied as an R
list.}

\item{json_list}{If \code{TRUE}, return the parsed JSON response as a list.}
}
\value{
A character string when Gemini returns text, a base64 string when the
first returned part is inline binary data, or the parsed JSON response when
\code{json_list = TRUE}.
}
\description{
Sends Gemini \code{generateContent} requests using either a simple text prompt or
an explicit list of content parts. This allows text, image, audio, PDF, and
other supported non-text inputs to be passed through the same wrapper.
}
